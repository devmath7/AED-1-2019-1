#include<iostream>
#define NIL -1

using namespace std;

int buscaSent(int v[], const int TAM){
	return 0;
}

void tentaBuscaSent(){
	int k = 1;
	int pos = buscaSent(k, v, TAM);
	int pos == NIL...
	else...
}

int main()
{
	const TAM = 5;
	int  v[] {1, 2, 3, 4, 5, NIL};

	int k = 0;
	cin >> k;
	buscaSent(k, v, TAM);

	return 0;
}